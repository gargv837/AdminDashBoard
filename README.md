# AdminDashBoard
This is a React web application that demonstrates a CRUD (Create, Read, Update, Delete) functionality using the Material-UI DataGrid component. It fetches sample data from an external API and allows users to search, edit, delete, and perform other actions on the displayed data.
